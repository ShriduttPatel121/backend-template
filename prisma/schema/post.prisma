model Post {
    id          Int      @id @default(autoincrement())
    user_id     Int
    title       String
    description String

    user        User     @relation(fields: [user_id], references: [id])

    @@index([user_id])
}